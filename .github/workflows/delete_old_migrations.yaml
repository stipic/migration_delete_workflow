name: Delete Old Migrations

on:
  schedule:
    - cron: "0 0 1 * *"
  workflow_dispatch:

jobs:
  delete-migrations:
    runs-on: ubuntu-latest
    permissions:
      contents: write
      pull-requests: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Set up Git user
        run: |
          git config --global user.email "github-actions[bot]@users.noreply.github.com"
          git config --global user.name "github-actions[bot]"

      - name: Delete old migrations
        run: |
          CURRENT_DATE=$(date +%s)
          for file in ./migrations/Version*; do
            FILENAME=$(basename "$file")
            TIMESTAMP=${FILENAME//[^0-9]/}
            FILE_DATE=$(date -d "$TIMESTAMP" +%s)
            AGE=$(( (CURRENT_DATE - FILE_DATE) / 86400 ))
            if [ "$AGE" -gt 14 ]; then
              echo "Deleting file: $file (Age: $AGE days)"
              rm "$file"
            fi
          done

      - name: Create Pull Request
        uses: peter-evans/create-pull-request@v7
        with:
          token: ${{ secrets.GITHUB_TOKEN }}
          commit-message: "Delete old migrations older than 14 days"
          committer: github-actions[bot] <41898282+github-actions[bot]@users.noreply.github.com>
          author: ${{ github.actor }} <${{ github.actor_id }}+${{ github.actor }}@users.noreply.github.com>
          signoff: false
          base: main  # The branch you're merging into
          branch: delete-old-migrations-$(date +%Y%m%d)  # The new branch name for the PR
          title: "Delete Old Migrations"
          body: |
            This pull request deletes migration files older than 14 days.
          draft: false
          delete-branch: true
